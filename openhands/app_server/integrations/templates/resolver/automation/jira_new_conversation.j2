You are working on Jira issue {{ issue_key }}.

Issue Key: {{ issue_key }}
Title: {{ title }}
Issue Type: {{ issue_type }}
Priority: {{ priority }}
Reporter: {{ reporter }}

Description:
{{ description }}

Target Repository: {{ repository }}
Default Branch: {{ default_branch }}
Working Branch: {{ branch }}

Requirements:
1. Analyze the Jira issue and understand the requested changes.
2. Determine whether **this** repository ("{{ repository }}") needs changes.
   - If the task only affects OTHER repositories, skip this repo entirely
     (still post a comment saying no changes were needed here).
   - If changes are needed, proceed with steps below.
3. Create a branch named "{{ branch }}" from "{{ default_branch }}".
4. Implement the required changes following the project coding standards.
5. Run relevant tests, linting, or validation commands whenever applicable.
6. Commit your changes with clear commit messages referencing the Jira issue key.
7. Create a pull request targeting "{{ default_branch }}".
8. Include a clear description of the implementation in the pull request.
9. After creating the pull request (or determining no changes are needed),
   post a comment on the Jira issue by sending a POST request to:
   POST {{ comment_endpoint }}
   {
     "issue_key": "{{ issue_key }}",
     "body": "Summary of changes implemented\\nKey files: ...\\nPR: <url>"
   }
