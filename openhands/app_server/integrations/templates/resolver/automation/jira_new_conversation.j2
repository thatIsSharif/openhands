{# Jira Automation Prompt - Pure Coding Task #}

# {{ title }}

**Issue**: {{ issue_key }}
**Type**: {{ issue_type }} | **Priority**: {{ priority }}

## Description

{{ description }}

## Working Context

- **Repository**: {{ repository }} ({{ repo_label }})
- **Working Branch**: {{ branch }} (already created and checked out)
- **Default Branch**: {{ default_branch }}

This repository is cloned at `/workspace/{{ repo_label or repository.split('/')[-1] }}`.
The working branch `{{ branch }}` is already checked out. All git operations,
PR creation, and Jira updates are handled automatically.

{% if other_repos %}
## Related Repositories

This issue may span multiple repositories. The following repos are also
available:

{% for repo in other_repos %}
- **{{ repo.owner }}/{{ repo.repository }}** ({{ repo.label }})
{% endfor %}

Only make changes in this repository ({{ repository }}) unless you are
absolutely certain changes are needed elsewhere.
{% endif %}

## Gate Rule

This repository is labeled "{{ repo_label }}".

Analyze the Jira issue thoroughly. Proceed only if changes are required in
**this** repository ({{ repository }}). Check the description for phrases like:
- "No {{ repo_label }} changes required"
- "{{ repo_label }} N/A" or "{{ repo_label }} changes: N/A"
- "Out of Scope: {{ repo_label }} changes"

If the issue explicitly excludes {{ repo_label }} work:
   → STOP. Do NOT make any changes.
   → Simply indicate that no changes are needed.

If the issue requires {{ repo_label }} changes:
   → Proceed with implementation below.

## Task

Implement the changes described in this Jira issue.

1. Read the issue description and understand the requirements
2. Make the necessary code changes
3. Run tests to verify your changes

## Important

- Do NOT perform any git operations (branch, commit, push, PR creation)
- Do NOT call any Jira or GitHub APIs
- Do NOT post comments or token usage reports
- These are all handled automatically by the automation platform
