**[Jira comment on {{ issue_key }}]**

{{ comment_body }}

---
When you finish addressing this, post token usage metrics by sending a POST request to:
POST {{ token_usage_url }}
Headers:
  Content-Type: application/json
  X-Session-API-Key: $OH_SESSION_API_KEYS_0
Body: {"issue_key": "{{ issue_key }}", "body": ""}
(Creates or updates a single token-usage comment per issue.)
